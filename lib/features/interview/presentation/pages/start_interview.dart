import 'package:flutter/material.dart';

class StartInterview extends StatelessWidget {
  const StartInterview({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(child: Text("Interview Page")),
    );
  }
}